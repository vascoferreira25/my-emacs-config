# -*- mode: snippet -*-
# name: unity_template
# key: unity
# group: unity
# --
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Use it for controlling the ui
// using UnityEngine.UI;

// Use it for AI stuff
// using UnityEngine.AI;

// Create a menu item
// [CreateAssetMenu(fileName = "New ItemName", menuName = "Parent/ItemName")]
// Create a component if needed
// [RequireComponent(typeof(ItemType))]
public class `(file-name-nondirectory (file-name-sans-extension buffer-file-name))` : MonoBehaviour
{
    /// <summary>
    /// Start is called before the first frame update
    /// </summary>
    void Start()
    {
        
    }

    /// <summary>
    /// Update is called once per frame
    /// </summary>
    void Update()
    {

    }
}
